#!/usr/bin/python3
def multiply_by_2(a_dictionary):
    new_dict = [(x, y) for x, y in a_dictionary.items()]
    dic = dict(new_dict)
    for k in dic:
        dic[k] = dic[k] * 2
    return new_dict
