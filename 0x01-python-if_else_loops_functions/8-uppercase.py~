#!/usr/bin/pyhton3
def uppercase(str):
    for i in str:
        if ord(i) in range(65, 91):
            continue
        else:
            i = chr(ord(i) - 32)
        print(f"{i}", end="")
    print("")
